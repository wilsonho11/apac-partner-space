{
  "name": "virtual-meeting-room",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "npm install --prefix server && node server/index.js",
    "dev": "concurrently -c \"cyan,green\" -n \"server,client\" \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
